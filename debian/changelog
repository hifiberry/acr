hifiberry-audiocontrol (0.7.18) trixie; urgency=medium

  * Expose stream_details for the lms and bluetooth players: lms reports
    codec/sample rate/bit depth via songinfo; bluetooth reports the A2DP
    codec (and SBC sample rate) from BlueZ MediaTransport1.
  * lms: emit a playing state change on the CLI newsong/play/stop events so the
    ActiveMonitor auto-switches to LMS when it starts playing.
 -- HiFiBerry <support@hifiberry.com>  Wed, 08 Jul 2026 17:00:00 +0000

