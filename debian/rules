#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	cargo build --release

override_dh_auto_install:
	mkdir -p debian/acr/usr/bin
	install -m 755 target/release/acr debian/acr/usr/bin/

override_dh_auto_clean:
	dh_auto_clean
	cargo clean
