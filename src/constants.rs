/// This file contains global constants used throughout the application
/// API prefix for all REST endpoints
///
/// Defined once in `acr-types` so the shared crates and the metadata daemon
/// use the same value rather than keeping copies of the string.
pub use acr_types::API_PREFIX;

// Add other global constants below as needed
